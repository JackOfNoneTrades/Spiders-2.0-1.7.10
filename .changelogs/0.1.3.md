* Renamed the mod to Nimble Spiders. The mod ID is now `nimble-spiders`, the package is `org.fentanylsolutions.nimblespiders`, and release jars are named NimbleSpiders.
* The config file moved to `config/nimble-spiders/nimble-spiders.cfg`. Settings from `config/spiderstpo/spiderstpo.cfg` are not migrated.
